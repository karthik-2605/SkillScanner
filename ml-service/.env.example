# Flask ML service
ML_PORT=5001

# MySQL connection (optional for training — used to fold job postings into the
# model. If unreachable, training falls back to the CSV dataset only.)
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=skillscanner
DB_NAME=skillScanner
